name: CI

on:
  push:
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    services:
      db:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: loyalty
          POSTGRES_PASSWORD: loyalty_local_only
          POSTGRES_DB: loyalty
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U loyalty -d loyalty"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    env:
      DATABASE_URL: postgres://loyalty:loyalty_local_only@localhost:5432/loyalty
      PUBLIC_WEB_URL: http://localhost:3000
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - run: npm test
      - run: npm run db:migrate
      - run: npm run test:integration -w @wallet/api
      - run: npm run build
